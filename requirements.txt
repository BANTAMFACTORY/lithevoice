# Installed by scripts/setup.ps1 after it selects a matching torch,
# torchaudio, and ONNX Runtime build for the machine.
numpy==2.5.0
soundfile==0.14.0
sounddevice==0.5.5
silero-vad==6.2.1
onnx-asr==0.11.0
kokoro==0.9.4
huggingface-hub==1.22.0
transformers==5.13.0
misaki[en]==0.9.4
# --phone serves the browser microphone over a WebSocket (phone_transport.py,
# docs/PHONE.md). Without this, --phone raises ModuleNotFoundError on connect.
websockets==15.0.1
